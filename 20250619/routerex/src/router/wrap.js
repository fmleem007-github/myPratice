import { BrowserRouter, Routes } from "react-router-dom";
import { SignIn } from "./member/Signin";
export const Wrap = () => {
    return(
        <BrowserRouter>

        <Routes>
            Wrap.jsx
        </Routes>
        
        </BrowserRouter>
    )
}